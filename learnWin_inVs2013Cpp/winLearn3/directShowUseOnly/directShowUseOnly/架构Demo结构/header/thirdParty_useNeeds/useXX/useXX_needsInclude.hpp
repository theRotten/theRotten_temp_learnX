#ifndef __useXX_needsInclude_hpp
#define __useXX_needsInclude_hpp
//该成分在该架构成分内当前结构版本 v0.4.1.2
//该成分在该结构成分所属架构位置描述：该独立[“外部组件调用提供集set”]构件，独立使用该第三方构件内容时需要的第三方内容包含指定。


//说明源自：https://这是一个列举的demo例子。。。。假设这是要调用一个叫作XX的独立第三方组件
#include<iostream>

#pragma comment(lib,"./lib/demoLib/LIBC.lib")

#endif
